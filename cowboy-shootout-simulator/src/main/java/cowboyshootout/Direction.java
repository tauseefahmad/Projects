package cowboyshootout;

/**
 * The two possible aiming directions around the circle.
 * RIGHT means "toward the next cowboy in seating order",
 * LEFT means "toward the previous cowboy in seating order".
 */
enum Direction {
    LEFT,
    RIGHT
}
